# Long-lived Instagram Graph API token (tied to an Instagram Business/Creator account linked to a Facebook Page)
IG_ACCESS_TOKEN=REPLACE_ME
# Optional: Instagram Business Account ID (if you use /{ig-user-id}/media)
IG_USER_ID=REPLACE_ME_IF_NEEDED
# Optional: Force base URL (only if relative fetch causes issues)
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
